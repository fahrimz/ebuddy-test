{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:be": "turbo run dev --filter=@ebuddy/backend-repo",
    "dev:fe": "turbo run dev --filter=@ebuddy/frontend-repo",
    "lint": "turbo run lint"
  },
  "packageManager": "pnpm@10.0.0",
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
